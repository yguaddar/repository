08:35 14.11.2022


pipeline {                                                // pipeline must be top level
    agent any                                               //  agent - where to execute 

    stages {  	// stages - where the work happens (stage / steps)
	
	    stage('Git - checkout') { // project must be automatically started
            steps {
                echo 'checking out from Git Repos'
				git branch: 'new_api', credentialsId:
            }
        }
	
        stage('build') {
            steps {
                echo 'building the application'
            }
        }
		
         stage('test') {
            steps {
                echo 'testing the application'
            }
        }
		
         stage('deploy') {
            steps {
                echo 'deploying the application'
            }
        }
    }
	
	post {                                             // Execute some logic after all stages executed
	
         always {                                      // conditions (always)
		     
		    }
		 
		 succes {                                        // relevant if the ebuild successed
		 
		 }
		 
		 failure {
		 
		 }
	}
	
}


http://github.com/freeautomation/CucumberFramework